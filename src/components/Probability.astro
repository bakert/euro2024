---
const { probability } = Astro.props
---
<span class="probability">{(probability * 100).toFixed(1).replace(/\.0*$/, "")}%</span>

<style>
    .probability {
        color: #999;
        font-size: var(--small-font-size);
    }
</style>
